import { studioPage } from "./admin-studio.mjs";

const esc = (value) => String(value ?? "").replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c]));
const labelSource = (value) => value || "Onbekend";
const list = (title, items, empty = "Nog geen gegevens") => `<section class="panel ranking"><h3>${esc(title)}</h3>${items.length ? `<ol>${items.map((item) => `<li><span title="${esc(item.label)}">${esc(labelSource(item.label))}</span><b>${item.count}</b></li>`).join("")}</ol>` : `<p class="empty">${esc(empty)}</p>`}</section>`;

export function webAnalyticsAdminPage(summary, profile) {
  const max = Math.max(1, ...summary.daily.map((item) => item.count));
  const chart = summary.daily.map((item) => `<i style="--h:${Math.max(3, Math.round(item.count / max * 100))}%" title="${esc(item.day)}: ${item.count} bezoeken"><span></span></i>`).join("");
  const range = [7, 30, 90].map((days) => `<a class="${summary.days === days ? "active" : ""}" href="/admin/analytics?days=${days}">${days} dagen</a>`).join("");
  return studioPage({ title: "Website-analyse", active: "analytics", profile,
    styles: `.analytics-range{display:flex;gap:6px}.analytics-range a{padding:8px 12px;border:1px solid var(--line);border-radius:999px;background:#fff;text-decoration:none;font-weight:750}.analytics-range a.active{border-color:var(--forest);background:var(--forest);color:#fff}.metric-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:14px;margin:18px 0}.metric{padding:21px}.metric small{display:block;color:var(--muted);font-weight:700}.metric strong{display:block;margin-top:4px;color:var(--forest);font:500 38px/1.1 Georgia,serif}.chart{height:190px;padding:20px;margin-bottom:14px}.chart h3,.ranking h3{margin:0 0 15px;color:var(--forest);font:500 22px Georgia,serif}.bars{height:120px;display:flex;align-items:end;gap:4px;border-bottom:1px solid var(--line)}.bars i{display:flex;align-items:end;flex:1;height:100%;min-width:2px}.bars span{display:block;width:100%;height:var(--h);min-height:3px;border-radius:4px 4px 0 0;background:linear-gradient(var(--gold),#e4cc77)}.ranking-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.ranking{padding:20px}.ranking ol{list-style:none;padding:0;margin:0}.ranking li{display:flex;align-items:center;gap:16px;padding:9px 0;border-top:1px solid var(--line)}.ranking li:first-child{border:0}.ranking li span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.ranking li b{margin-left:auto;color:var(--forest)}.privacy-note{margin-top:15px;padding:14px 17px;border:1px solid #d8e5df;border-radius:13px;background:var(--forest-soft);color:var(--forest)}.empty{color:var(--muted)}@media(max-width:780px){.metric-grid,.ranking-grid{grid-template-columns:1fr}.page-intro{display:block}.analytics-range{margin-top:14px}}`,
    content: `<section class="page-intro"><div><h2>Website-analyse</h2><p>Inzicht in bezoeken, populaire pagina’s en waar bezoekers vandaan komen.</p></div><nav class="analytics-range">${range}</nav></section><section class="metric-grid"><article class="panel metric"><small>Bezoeken</small><strong>${summary.pageViews}</strong></article><article class="panel metric"><small>Unieke bezoekers</small><strong>${summary.uniqueVisitors}</strong></article><article class="panel metric"><small>Pagina’s per bezoeker</small><strong>${summary.pagesPerVisitor}</strong></article></section><section class="panel chart"><h3>Bezoeken per dag</h3><div class="bars">${chart}</div></section><section class="ranking-grid">${list("Populaire pagina’s", summary.pages)}${list("Verkeersbronnen", summary.sources)}${list("Verwijzende websites", summary.referrers)}${list("Apparaten", summary.devices)}${list("Browsertaal", summary.languages)}${list("Landen", summary.countries, "Landgegevens worden zichtbaar zodra de server een betrouwbare landcode ontvangt.")}</section><p class="privacy-note"><strong>Privacyvriendelijk:</strong> Champagne Atlas bewaart geen ruwe IP-adressen en gebruikt hiervoor geen advertentiecookies. Unieke bezoekers worden met een dagelijks wisselende, niet-terug te rekenen hash geschat.</p>` });
}
